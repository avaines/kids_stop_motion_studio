{
  "name": "kids-stop-motion-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private, browser-based stop-motion studio for young children.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check src/app.js && node --check src/gif-encoder.js",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
